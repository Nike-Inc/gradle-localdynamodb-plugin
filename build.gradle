plugins {
    id "com.jfrog.bintray" version "1.7.3"
    id "pl.droidsonroids.jacoco.testkit" version "1.0.3"
}

apply plugin: 'java'
apply plugin: 'maven'
apply plugin: 'groovy'
apply plugin: 'java-gradle-plugin'

group = 'com.nike.localdynamodb'

apply from: file('gradle/convention.gradle')
apply from: file('gradle/release.gradle')

repositories {
  jcenter()
  maven { url "http://dynamodb-local.s3-website-us-west-2.amazonaws.com/release" }
}

dependencies {
    compile gradleApi()
    compile localGroovy()
    compile('com.amazonaws:DynamoDBLocal:1.10.5.1') {
        exclude group: "org.slf4j", module: "slf4j-log4j12"
    }
    compile "org.slf4j:jcl-over-slf4j:1.7.5"
    compile "org.slf4j:slf4j-simple:1.7.5"
    testCompile("org.spockframework:spock-core:1.1-groovy-2.4") {
        exclude module: 'groovy-all'
    }
    testCompile group: 'com.amazonaws', name: 'aws-java-sdk-dynamodb', version: '1.11.220'
}

tasks.withType(JavaCompile) {
    sourceCompatibility = "1.8"
    targetCompatibility = "1.8"
}
